export const DEFAULT_PAGE_SIZE = 10000;
export const DEFAULT_PANEL_WIDTH = 340;
export const DEFAULT_TITLE = 'GeoParquet';

export const ALLOWED_BINARY_OPERATORS = new Set(['=', '!=', '>', '>=', '<', '<=']);
export const ALLOWED_UNARY_OPERATORS = new Set([
  'IS NULL',
  'IS NOT NULL',
  'IS TRUE',
  'IS NOT TRUE',
  'IS FALSE',
  'IS NOT FALSE',
]);
